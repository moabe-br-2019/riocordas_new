{
  "name": "riocordas-astro",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "dev:local": "BASEROW_API_TOKEN=$(cat .env.local | grep BASEROW_API_TOKEN | cut -d '=' -f2) astro dev",
    "start": "astro dev",
    "build": "astro build",
    "build:check": "astro check && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^8.1.0",
    "astro": "^4.16.18"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/sitemap": "^3.6.0",
    "prettier": "^3.6.2",
    "typescript": "^5.6.3"
  }
}
